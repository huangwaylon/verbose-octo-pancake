# The format gate globs **/*.{js,jsx}, so only entries matching a .js or .jsx file
# matter here. The stylesheets and docs are hand-tuned and already outside the glob.
# dist/ does need an entry: dist/sw.js is generated JavaScript, so the glob reaches it.
dist/
