{
  "name": "sharedfinances",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared expense tracker for two people, backed by a Google Sheet.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-sw.js",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write \"**/*.{js,jsx}\"",
    "format:check": "prettier --check \"**/*.{js,jsx}\""
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "prettier": "^3.9.6",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
